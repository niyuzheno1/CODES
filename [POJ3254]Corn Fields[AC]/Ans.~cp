#include "mode.h"

#define mod 100000000 
int M,N,top = 0;
int state[600],num[110];
int dp[20][500];
int cur[20];

inline bool ok(int x){
 if(x&x<<1) return 0;
 return 1;
}

int main(int argc, char *argv[])
{
    setIO("sample");
    
    closeIO();
    return EXIT_SUCCESS;
}
