#include "mode.h"
#include "spj.h"
/*A :in
  B :ans
  C :out
  D :res
*/
int main()
{
    setspj();
    int c = 1;
    while(1)
    {
     int x,y;
     int flag1 = (~fr(B,"%d",&x)),flag2 = (~fr(C,"%d",&y));
     if(x != y)
      ff(D,"%d\n",c);
     ++c;
    }
    closespj();
    return 0;
}
